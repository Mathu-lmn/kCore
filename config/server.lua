return {
    StartingMoney = 1000,
    StartingBank = 1000,
    SaveInterval = 10000 ,
    StartingPosition = { -- will be changed later on probably because of spawn point picker?
        x = -1042.0345,
        y = -2744.7874,
        z = 20.3594,
        heading = 325.2877
    },
    MaxCharacterSlots = 3,
    AutoloadChar = false,  -- autoload char if Config.MaxCharacterSlots == 1
    PaycheckInterval = 1000 * 60 * 60 -- paycheck interval in ms, nil to disable
}

